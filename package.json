{
  "name": "draw-app",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "setup": "cd packages/db && pnpm install && pnpm exec prisma migrate dev && pnpm exec prisma generate && cd ../../apps/http-backend && pnpm install && cd ../ws-backend && pnpm install && cd ../excelidraw-frontend && pnpm install",
    "start-all": "cd apps/http-backend && pnpm run dev & cd ../ws-backend && pnpm run dev & cd ../excelidraw-frontend && pnpm run dev"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.3.3",
    "typescript": "5.5.4"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
